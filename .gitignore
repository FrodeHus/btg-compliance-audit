# macOS
.DS_Store
**/.DS_Store

# Terraform
.terraform/
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan*
crash.log
*.tfvars
!terraform/terraform.tfvars.example
.terraform.tfstate.lock.info

# Keep the lock file tracked so builds are reproducible and CI remains deterministic.
# Local state is the default for this repo because BTG account identifiers may appear in state.

# Editor / tooling
.vscode/
.idea/
*.swp
*.swo

# Local artifacts
*.log

# Output folders
out/
